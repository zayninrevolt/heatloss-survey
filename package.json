{
  "name": "heatloss-survey",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:browser": "playwright test",
    "check": "node --check combined-heatloss.js && for file in src/*.js; do node --check \"$file\" || exit 1; done"
  }
}
