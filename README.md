# Heatloss Survey

A separate working copy of the heating survey web app for adding room-by-room heat-loss calculations.

The app is self-contained in `index.html` and stores survey data locally in the browser.

Source project: https://github.com/zayninrevolt/survey
